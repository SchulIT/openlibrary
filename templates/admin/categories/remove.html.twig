{% extends "main.html.twig" %}

{% block title %}{{ 'categories.remove.label'|trans }}{% endblock %}

{% block breadcrumb %}
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="{{ path('dashboard') }}">{{ 'dashboard.menu'|trans }}</a></li>
        <li class="breadcrumb-item"><a href="{{ path('admin_categories') }}">{{ 'categories.menu'|trans }}</a></li>
        <li class="breadcrumb-item active">{{ block('title') }}</li>
    </ol>
{% endblock %}

{% block content %}
    {{ form_start(form) }}

    <div class="card border-danger">
        <div class="card-header text-bg-danger">{{ 'categories.remove.label'|trans }}</div>
        <div class="card-body">
            {{ form_widget(form.group_general.confirm) }}
            {{ form_row(form.category) }}
        </div>
    </div>

    <div class="buttons">
        <button type="submit" class="btn btn-sm btn-danger">
            <i class="fas fa-save"></i> {{ 'actions.remove'|trans }}
        </button>

        <a href="javascript:history.back();" class="btn btn-sm btn-secondary">
            <i class="fas fa-arrow-left"></i> {{ 'actions.cancel'|trans }}
        </a>
    </div>
    {{ form_end(form) }}

{% endblock %}
